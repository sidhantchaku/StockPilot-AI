# Copy this into Streamlit Cloud -> App -> Settings -> Secrets
# Required for persistent storage (recommended)
DATABASE_URL = "postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME"

# Optional local/dev fallback (not for production)
# DATABASE_URL = "sqlite:///stockpilot.db"
